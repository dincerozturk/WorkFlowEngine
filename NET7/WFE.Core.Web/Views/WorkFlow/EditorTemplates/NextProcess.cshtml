@model WorkFlowManager.Common.ViewModels.NextProcess

@Html.DropDownListFor(m => m.Process.NextProcessId, new SelectList(Model.MainProcessList, "Id", "Name")
    , "Select", new { Id = Model.Process.ProcessUniqueCode, onchange = "UserSelectionChanged("+(Model.Process.NextProcessId != null? Model.Process.NextProcessId.ToString() : "\"\"") + ", this);" })

